# Example workspace

Place supplier **`.eps`** or **`.ai`** files in **`supplier/`**, then run **`run-batch-now.sh`** with this folder and your property code.

This directory ships empty (`.gitkeep`) so the repo stays tooling-only.
